#This is how we calculate the discriminate
def calculate_discriminate(a, b, c):
    return b**2 - 4*a*c
#Returns the discriminate to the user in the terminal
print(calculate_discriminate(1, -5, 6))
print(calculate_roots(1, 2, 1))   # Should print (-1.0, -1.0)